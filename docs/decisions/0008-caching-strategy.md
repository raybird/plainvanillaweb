# ADR 0008: 智能快取策略 (Caching Strategy)

## 上下文
為了減少對外部 API（如 GitHub）的頻繁請求，並加速使用者回訪時的體驗，需要一套標準的快取機制。

## 決策
在 `Store` 中實作基於 TTL 的快取管理方法，並優先使用 `LocalStorage` 進行持久化。

## 後果
- **優點**: 大幅降低 API 消耗，對抗 429 錯誤。
- **優點**: 離線友善，即便重新整理頁面，最近的搜尋結果也能立即顯示。
- **缺點**: 若資料過時（在 TTL 到期前），使用者可能看到舊數據。
