# 🪄 View Transitions 過渡動畫

`View Transitions API` 可讓 SPA 在切頁或狀態改變時保有流暢視覺連續性，避免瞬間切換造成突兀感。

## 🌟 為什麼用 View Transitions

- **體驗更連續**：頁面或元件更新時有自然過渡。
- **原生能力**：不需要第三方動畫框架，維持 Vanilla-first。
- **漸進增強**：不支援瀏覽器仍可正常運作。

## 🧩 基本用法

```javascript
if (document.startViewTransition) {
  document.startViewTransition(() => {
    updateUI();
  });
} else {
  updateUI();
}
```

## 🛡️ 相容性與降級策略

- 先檢查：`typeof document.startViewTransition === "function"`
- 不支援時直接執行更新邏輯，功能不受影響
- 動畫樣式可透過 `::view-transition-old(root)` / `::view-transition-new(root)` 調整

## 🎓 教學對應

在 `Lab` 的 `#/lab/view-transitions` 可直接體驗：

1. 切換版面與色盤，觀察同頁狀態過渡
2. 跳轉到其他 Lab 或 Docs，觀察路由過渡
3. 查看支援狀態與 fallback 行為
